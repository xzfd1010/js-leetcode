import { reverseWordsInPlace } from '../../code/basic-string/reverseWordsInPlace'

test('reverseWords:the sky is blue', () => {
  expect(reverseWordsInPlace('the sky is blue')).toBe('blue is sky the')
})
